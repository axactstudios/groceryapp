class User {
  String uid, name, address, zip, phoneNo, lat, lng;

  User(
      {this.uid,
      this.name,
      this.address,
      this.lng,
      this.lat,
      this.zip,
      this.phoneNo});
}
